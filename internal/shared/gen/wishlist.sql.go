// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: wishlist.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const addToWishlist = `-- name: AddToWishlist :one
INSERT INTO wishlist_items (user_id, product_id)
VALUES ($1, $2)
ON CONFLICT (user_id, product_id) DO NOTHING
RETURNING id, user_id, product_id, created_at
`

type AddToWishlistParams struct {
	UserID    int32
	ProductID pgtype.UUID
}

func (q *Queries) AddToWishlist(ctx context.Context, arg AddToWishlistParams) (WishlistItem, error) {
	row := q.db.QueryRow(ctx, addToWishlist, arg.UserID, arg.ProductID)
	var i WishlistItem
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.ProductID,
		&i.CreatedAt,
	)
	return i, err
}

const clearWishlist = `-- name: ClearWishlist :exec
DELETE FROM wishlist_items WHERE user_id = $1
`

func (q *Queries) ClearWishlist(ctx context.Context, userID int32) error {
	_, err := q.db.Exec(ctx, clearWishlist, userID)
	return err
}

const getWishlistItem = `-- name: GetWishlistItem :one
SELECT wi.id, wi.user_id, wi.product_id, wi.created_at, p.title, p.price, p.original_price, p.images, p.stock, p.status,
       u.first_name || ' ' || u.last_name as seller_name,
       s.name as store_name, s.logo as store_logo, s.rating as store_rating
FROM wishlist_items wi
LEFT JOIN products p ON wi.product_id = p.id
LEFT JOIN users u ON p.seller_id = u.id
LEFT JOIN stores s ON s.user_id = p.seller_id
WHERE wi.user_id = $1 AND wi.product_id = $2
`

type GetWishlistItemParams struct {
	UserID    int32
	ProductID pgtype.UUID
}

type GetWishlistItemRow struct {
	ID            pgtype.UUID
	UserID        int32
	ProductID     pgtype.UUID
	CreatedAt     pgtype.Timestamptz
	Title         pgtype.Text
	Price         pgtype.Numeric
	OriginalPrice pgtype.Numeric
	Images        []string
	Stock         pgtype.Int4
	Status        pgtype.Text
	SellerName    interface{}
	StoreName     pgtype.Text
	StoreLogo     pgtype.Text
	StoreRating   pgtype.Numeric
}

func (q *Queries) GetWishlistItem(ctx context.Context, arg GetWishlistItemParams) (GetWishlistItemRow, error) {
	row := q.db.QueryRow(ctx, getWishlistItem, arg.UserID, arg.ProductID)
	var i GetWishlistItemRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.ProductID,
		&i.CreatedAt,
		&i.Title,
		&i.Price,
		&i.OriginalPrice,
		&i.Images,
		&i.Stock,
		&i.Status,
		&i.SellerName,
		&i.StoreName,
		&i.StoreLogo,
		&i.StoreRating,
	)
	return i, err
}

const getWishlistItemCount = `-- name: GetWishlistItemCount :one
SELECT COUNT(*) FROM wishlist_items WHERE user_id = $1
`

func (q *Queries) GetWishlistItemCount(ctx context.Context, userID int32) (int64, error) {
	row := q.db.QueryRow(ctx, getWishlistItemCount, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const isInWishlist = `-- name: IsInWishlist :one
SELECT EXISTS(
    SELECT 1 FROM wishlist_items 
    WHERE user_id = $1 AND product_id = $2
) as is_in_wishlist
`

type IsInWishlistParams struct {
	UserID    int32
	ProductID pgtype.UUID
}

func (q *Queries) IsInWishlist(ctx context.Context, arg IsInWishlistParams) (bool, error) {
	row := q.db.QueryRow(ctx, isInWishlist, arg.UserID, arg.ProductID)
	var is_in_wishlist bool
	err := row.Scan(&is_in_wishlist)
	return is_in_wishlist, err
}

const listWishlistItems = `-- name: ListWishlistItems :many
SELECT wi.id, wi.user_id, wi.product_id, wi.created_at, p.title, p.price, p.original_price, p.images, p.stock, p.status,
       u.first_name || ' ' || u.last_name as seller_name,
       s.name as store_name, s.logo as store_logo, s.rating as store_rating
FROM wishlist_items wi
LEFT JOIN products p ON wi.product_id = p.id
LEFT JOIN users u ON p.seller_id = u.id
LEFT JOIN stores s ON s.user_id = p.seller_id
WHERE wi.user_id = $1
ORDER BY wi.created_at DESC
LIMIT $2 OFFSET $3
`

type ListWishlistItemsParams struct {
	UserID int32
	Limit  int32
	Offset int32
}

type ListWishlistItemsRow struct {
	ID            pgtype.UUID
	UserID        int32
	ProductID     pgtype.UUID
	CreatedAt     pgtype.Timestamptz
	Title         pgtype.Text
	Price         pgtype.Numeric
	OriginalPrice pgtype.Numeric
	Images        []string
	Stock         pgtype.Int4
	Status        pgtype.Text
	SellerName    interface{}
	StoreName     pgtype.Text
	StoreLogo     pgtype.Text
	StoreRating   pgtype.Numeric
}

func (q *Queries) ListWishlistItems(ctx context.Context, arg ListWishlistItemsParams) ([]ListWishlistItemsRow, error) {
	rows, err := q.db.Query(ctx, listWishlistItems, arg.UserID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListWishlistItemsRow{}
	for rows.Next() {
		var i ListWishlistItemsRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.ProductID,
			&i.CreatedAt,
			&i.Title,
			&i.Price,
			&i.OriginalPrice,
			&i.Images,
			&i.Stock,
			&i.Status,
			&i.SellerName,
			&i.StoreName,
			&i.StoreLogo,
			&i.StoreRating,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const removeFromWishlist = `-- name: RemoveFromWishlist :exec
DELETE FROM wishlist_items WHERE user_id = $1 AND product_id = $2
`

type RemoveFromWishlistParams struct {
	UserID    int32
	ProductID pgtype.UUID
}

func (q *Queries) RemoveFromWishlist(ctx context.Context, arg RemoveFromWishlistParams) error {
	_, err := q.db.Exec(ctx, removeFromWishlist, arg.UserID, arg.ProductID)
	return err
}
