// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: addresses.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createAddress = `-- name: CreateAddress :one
INSERT INTO addresses (
    user_id, street, city, state, zip_code, country, is_default
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
) RETURNING id, user_id, street, city, state, zip_code, country, is_default, created_at, updated_at
`

type CreateAddressParams struct {
	UserID    int32
	Street    string
	City      string
	State     string
	ZipCode   string
	Country   string
	IsDefault pgtype.Bool
}

func (q *Queries) CreateAddress(ctx context.Context, arg CreateAddressParams) (Address, error) {
	row := q.db.QueryRow(ctx, createAddress,
		arg.UserID,
		arg.Street,
		arg.City,
		arg.State,
		arg.ZipCode,
		arg.Country,
		arg.IsDefault,
	)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Street,
		&i.City,
		&i.State,
		&i.ZipCode,
		&i.Country,
		&i.IsDefault,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteAddress = `-- name: DeleteAddress :exec
DELETE FROM addresses WHERE id = $1
`

func (q *Queries) DeleteAddress(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteAddress, id)
	return err
}

const getAddress = `-- name: GetAddress :one
SELECT id, user_id, street, city, state, zip_code, country, is_default, created_at, updated_at FROM addresses WHERE id = $1
`

func (q *Queries) GetAddress(ctx context.Context, id pgtype.UUID) (Address, error) {
	row := q.db.QueryRow(ctx, getAddress, id)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Street,
		&i.City,
		&i.State,
		&i.ZipCode,
		&i.Country,
		&i.IsDefault,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getAddressCount = `-- name: GetAddressCount :one
SELECT COUNT(*) FROM addresses WHERE user_id = $1
`

func (q *Queries) GetAddressCount(ctx context.Context, userID int32) (int64, error) {
	row := q.db.QueryRow(ctx, getAddressCount, userID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const getDefaultAddress = `-- name: GetDefaultAddress :one
SELECT id, user_id, street, city, state, zip_code, country, is_default, created_at, updated_at FROM addresses 
WHERE user_id = $1 AND is_default = true
LIMIT 1
`

func (q *Queries) GetDefaultAddress(ctx context.Context, userID int32) (Address, error) {
	row := q.db.QueryRow(ctx, getDefaultAddress, userID)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Street,
		&i.City,
		&i.State,
		&i.ZipCode,
		&i.Country,
		&i.IsDefault,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listUserAddresses = `-- name: ListUserAddresses :many
SELECT id, user_id, street, city, state, zip_code, country, is_default, created_at, updated_at FROM addresses 
WHERE user_id = $1
ORDER BY is_default DESC, created_at DESC
`

func (q *Queries) ListUserAddresses(ctx context.Context, userID int32) ([]Address, error) {
	rows, err := q.db.Query(ctx, listUserAddresses, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Address{}
	for rows.Next() {
		var i Address
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Street,
			&i.City,
			&i.State,
			&i.ZipCode,
			&i.Country,
			&i.IsDefault,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setDefaultAddress = `-- name: SetDefaultAddress :exec
UPDATE addresses 
SET is_default = CASE WHEN id = $2 THEN true ELSE false END,
    updated_at = NOW()
WHERE user_id = $1
`

type SetDefaultAddressParams struct {
	UserID int32
	ID     pgtype.UUID
}

func (q *Queries) SetDefaultAddress(ctx context.Context, arg SetDefaultAddressParams) error {
	_, err := q.db.Exec(ctx, setDefaultAddress, arg.UserID, arg.ID)
	return err
}

const updateAddress = `-- name: UpdateAddress :one
UPDATE addresses 
SET street = $2, city = $3, state = $4, zip_code = $5, 
    country = $6, is_default = $7, updated_at = NOW()
WHERE id = $1
RETURNING id, user_id, street, city, state, zip_code, country, is_default, created_at, updated_at
`

type UpdateAddressParams struct {
	ID        pgtype.UUID
	Street    string
	City      string
	State     string
	ZipCode   string
	Country   string
	IsDefault pgtype.Bool
}

func (q *Queries) UpdateAddress(ctx context.Context, arg UpdateAddressParams) (Address, error) {
	row := q.db.QueryRow(ctx, updateAddress,
		arg.ID,
		arg.Street,
		arg.City,
		arg.State,
		arg.ZipCode,
		arg.Country,
		arg.IsDefault,
	)
	var i Address
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Street,
		&i.City,
		&i.State,
		&i.ZipCode,
		&i.Country,
		&i.IsDefault,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
