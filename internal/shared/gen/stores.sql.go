// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: stores.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createStore = `-- name: CreateStore :one
INSERT INTO stores (
    user_id, name, description, logo, banner, address, phone, email, is_verified
) VALUES (
    $1, $2, $3, $4, $5, $6, $7, $8, $9
) RETURNING id, user_id, name, description, logo, banner, address, phone, email, is_verified, rating, review_count, created_at, updated_at
`

type CreateStoreParams struct {
	UserID      int32
	Name        string
	Description pgtype.Text
	Logo        pgtype.Text
	Banner      pgtype.Text
	Address     pgtype.Text
	Phone       pgtype.Text
	Email       pgtype.Text
	IsVerified  pgtype.Bool
}

func (q *Queries) CreateStore(ctx context.Context, arg CreateStoreParams) (Store, error) {
	row := q.db.QueryRow(ctx, createStore,
		arg.UserID,
		arg.Name,
		arg.Description,
		arg.Logo,
		arg.Banner,
		arg.Address,
		arg.Phone,
		arg.Email,
		arg.IsVerified,
	)
	var i Store
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Description,
		&i.Logo,
		&i.Banner,
		&i.Address,
		&i.Phone,
		&i.Email,
		&i.IsVerified,
		&i.Rating,
		&i.ReviewCount,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteStore = `-- name: DeleteStore :exec
DELETE FROM stores WHERE id = $1
`

func (q *Queries) DeleteStore(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteStore, id)
	return err
}

const getStore = `-- name: GetStore :one
SELECT s.id, s.user_id, s.name, s.description, s.logo, s.banner, s.address, s.phone, s.email, s.is_verified, s.rating, s.review_count, s.created_at, s.updated_at, u.first_name || ' ' || u.last_name as owner_name, u.email as owner_email
FROM stores s
LEFT JOIN users u ON s.user_id = u.id
WHERE s.id = $1
`

type GetStoreRow struct {
	ID          pgtype.UUID
	UserID      int32
	Name        string
	Description pgtype.Text
	Logo        pgtype.Text
	Banner      pgtype.Text
	Address     pgtype.Text
	Phone       pgtype.Text
	Email       pgtype.Text
	IsVerified  pgtype.Bool
	Rating      pgtype.Numeric
	ReviewCount pgtype.Int4
	CreatedAt   pgtype.Timestamptz
	UpdatedAt   pgtype.Timestamptz
	OwnerName   interface{}
	OwnerEmail  pgtype.Text
}

func (q *Queries) GetStore(ctx context.Context, id pgtype.UUID) (GetStoreRow, error) {
	row := q.db.QueryRow(ctx, getStore, id)
	var i GetStoreRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Description,
		&i.Logo,
		&i.Banner,
		&i.Address,
		&i.Phone,
		&i.Email,
		&i.IsVerified,
		&i.Rating,
		&i.ReviewCount,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.OwnerName,
		&i.OwnerEmail,
	)
	return i, err
}

const getStoreByUserID = `-- name: GetStoreByUserID :one
SELECT s.id, s.user_id, s.name, s.description, s.logo, s.banner, s.address, s.phone, s.email, s.is_verified, s.rating, s.review_count, s.created_at, s.updated_at, u.first_name || ' ' || u.last_name as owner_name, u.email as owner_email
FROM stores s
LEFT JOIN users u ON s.user_id = u.id
WHERE s.user_id = $1
`

type GetStoreByUserIDRow struct {
	ID          pgtype.UUID
	UserID      int32
	Name        string
	Description pgtype.Text
	Logo        pgtype.Text
	Banner      pgtype.Text
	Address     pgtype.Text
	Phone       pgtype.Text
	Email       pgtype.Text
	IsVerified  pgtype.Bool
	Rating      pgtype.Numeric
	ReviewCount pgtype.Int4
	CreatedAt   pgtype.Timestamptz
	UpdatedAt   pgtype.Timestamptz
	OwnerName   interface{}
	OwnerEmail  pgtype.Text
}

func (q *Queries) GetStoreByUserID(ctx context.Context, userID int32) (GetStoreByUserIDRow, error) {
	row := q.db.QueryRow(ctx, getStoreByUserID, userID)
	var i GetStoreByUserIDRow
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Description,
		&i.Logo,
		&i.Banner,
		&i.Address,
		&i.Phone,
		&i.Email,
		&i.IsVerified,
		&i.Rating,
		&i.ReviewCount,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.OwnerName,
		&i.OwnerEmail,
	)
	return i, err
}

const getStoreStats = `-- name: GetStoreStats :one
SELECT 
    COUNT(*) as total_stores,
    COUNT(CASE WHEN is_verified = true THEN 1 END) as verified_stores,
    AVG(rating) as average_rating,
    SUM(review_count) as total_reviews
FROM stores
`

type GetStoreStatsRow struct {
	TotalStores    int64
	VerifiedStores int64
	AverageRating  float64
	TotalReviews   int64
}

func (q *Queries) GetStoreStats(ctx context.Context) (GetStoreStatsRow, error) {
	row := q.db.QueryRow(ctx, getStoreStats)
	var i GetStoreStatsRow
	err := row.Scan(
		&i.TotalStores,
		&i.VerifiedStores,
		&i.AverageRating,
		&i.TotalReviews,
	)
	return i, err
}

const listStores = `-- name: ListStores :many
SELECT s.id, s.user_id, s.name, s.description, s.logo, s.banner, s.address, s.phone, s.email, s.is_verified, s.rating, s.review_count, s.created_at, s.updated_at, u.first_name || ' ' || u.last_name as owner_name, u.email as owner_email
FROM stores s
LEFT JOIN users u ON s.user_id = u.id
WHERE s.is_verified = true
ORDER BY s.rating DESC, s.review_count DESC
LIMIT $1 OFFSET $2
`

type ListStoresParams struct {
	Limit  int32
	Offset int32
}

type ListStoresRow struct {
	ID          pgtype.UUID
	UserID      int32
	Name        string
	Description pgtype.Text
	Logo        pgtype.Text
	Banner      pgtype.Text
	Address     pgtype.Text
	Phone       pgtype.Text
	Email       pgtype.Text
	IsVerified  pgtype.Bool
	Rating      pgtype.Numeric
	ReviewCount pgtype.Int4
	CreatedAt   pgtype.Timestamptz
	UpdatedAt   pgtype.Timestamptz
	OwnerName   interface{}
	OwnerEmail  pgtype.Text
}

func (q *Queries) ListStores(ctx context.Context, arg ListStoresParams) ([]ListStoresRow, error) {
	rows, err := q.db.Query(ctx, listStores, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListStoresRow{}
	for rows.Next() {
		var i ListStoresRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Description,
			&i.Logo,
			&i.Banner,
			&i.Address,
			&i.Phone,
			&i.Email,
			&i.IsVerified,
			&i.Rating,
			&i.ReviewCount,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.OwnerName,
			&i.OwnerEmail,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listVerifiedStores = `-- name: ListVerifiedStores :many
SELECT s.id, s.user_id, s.name, s.description, s.logo, s.banner, s.address, s.phone, s.email, s.is_verified, s.rating, s.review_count, s.created_at, s.updated_at, u.first_name || ' ' || u.last_name as owner_name, u.email as owner_email
FROM stores s
LEFT JOIN users u ON s.user_id = u.id
WHERE s.is_verified = true
ORDER BY s.rating DESC, s.review_count DESC
LIMIT $1 OFFSET $2
`

type ListVerifiedStoresParams struct {
	Limit  int32
	Offset int32
}

type ListVerifiedStoresRow struct {
	ID          pgtype.UUID
	UserID      int32
	Name        string
	Description pgtype.Text
	Logo        pgtype.Text
	Banner      pgtype.Text
	Address     pgtype.Text
	Phone       pgtype.Text
	Email       pgtype.Text
	IsVerified  pgtype.Bool
	Rating      pgtype.Numeric
	ReviewCount pgtype.Int4
	CreatedAt   pgtype.Timestamptz
	UpdatedAt   pgtype.Timestamptz
	OwnerName   interface{}
	OwnerEmail  pgtype.Text
}

func (q *Queries) ListVerifiedStores(ctx context.Context, arg ListVerifiedStoresParams) ([]ListVerifiedStoresRow, error) {
	rows, err := q.db.Query(ctx, listVerifiedStores, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListVerifiedStoresRow{}
	for rows.Next() {
		var i ListVerifiedStoresRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Name,
			&i.Description,
			&i.Logo,
			&i.Banner,
			&i.Address,
			&i.Phone,
			&i.Email,
			&i.IsVerified,
			&i.Rating,
			&i.ReviewCount,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.OwnerName,
			&i.OwnerEmail,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateStore = `-- name: UpdateStore :one
UPDATE stores 
SET name = $2, description = $3, logo = $4, banner = $5, 
    address = $6, phone = $7, email = $8, is_verified = $9, updated_at = NOW()
WHERE id = $1
RETURNING id, user_id, name, description, logo, banner, address, phone, email, is_verified, rating, review_count, created_at, updated_at
`

type UpdateStoreParams struct {
	ID          pgtype.UUID
	Name        string
	Description pgtype.Text
	Logo        pgtype.Text
	Banner      pgtype.Text
	Address     pgtype.Text
	Phone       pgtype.Text
	Email       pgtype.Text
	IsVerified  pgtype.Bool
}

func (q *Queries) UpdateStore(ctx context.Context, arg UpdateStoreParams) (Store, error) {
	row := q.db.QueryRow(ctx, updateStore,
		arg.ID,
		arg.Name,
		arg.Description,
		arg.Logo,
		arg.Banner,
		arg.Address,
		arg.Phone,
		arg.Email,
		arg.IsVerified,
	)
	var i Store
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Description,
		&i.Logo,
		&i.Banner,
		&i.Address,
		&i.Phone,
		&i.Email,
		&i.IsVerified,
		&i.Rating,
		&i.ReviewCount,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateStoreRating = `-- name: UpdateStoreRating :exec
UPDATE stores 
SET rating = $2, review_count = $3, updated_at = NOW()
WHERE id = $1
`

type UpdateStoreRatingParams struct {
	ID          pgtype.UUID
	Rating      pgtype.Numeric
	ReviewCount pgtype.Int4
}

func (q *Queries) UpdateStoreRating(ctx context.Context, arg UpdateStoreRatingParams) error {
	_, err := q.db.Exec(ctx, updateStoreRating, arg.ID, arg.Rating, arg.ReviewCount)
	return err
}

const verifyStore = `-- name: VerifyStore :one
UPDATE stores 
SET is_verified = true, updated_at = NOW()
WHERE id = $1
RETURNING id, user_id, name, description, logo, banner, address, phone, email, is_verified, rating, review_count, created_at, updated_at
`

func (q *Queries) VerifyStore(ctx context.Context, id pgtype.UUID) (Store, error) {
	row := q.db.QueryRow(ctx, verifyStore, id)
	var i Store
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Name,
		&i.Description,
		&i.Logo,
		&i.Banner,
		&i.Address,
		&i.Phone,
		&i.Email,
		&i.IsVerified,
		&i.Rating,
		&i.ReviewCount,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
